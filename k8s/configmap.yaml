apiVersion: v1
kind: ConfigMap
metadata:
  name: vizcore-config
  labels:
    app: vizcore
data:
  NODE_ENV: "production"
  LOG_LEVEL: "info"
  MEMORY_THRESHOLD: "80"
  PERFORMANCE_MONITORING: "true"
